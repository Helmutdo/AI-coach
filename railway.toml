# Despliegue del backend FastAPI desde monorepo (raíz del repo).
# Ver también: Settings → Root Directory = vacío o ".", build usa este Dockerfile.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
